import * as React from 'react';

interface IRemindersProps {}

const Reminders: React.FunctionComponent<IRemindersProps> = (props) => {
  return (
    <>
      <h1>Reminder page</h1>
    </>
  );
};

export default Reminders;
